@page
@using ItMe.Utils
@using ViewComponents
@inject GlobalsManager Globals
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

@if (Globals.Person != null)
{
    @await Component.InvokeAsync("Blog")
}
